# Convex
NEXT_PUBLIC_CONVEX_URL=

# Auth (Resend for magic links)
# Set these in Convex Dashboard or via `npx convex env set`
# SITE_URL=http://localhost:3000  (set in Convex env)
# AUTH_RESEND_KEY=                (set in Convex env)
AUTH_EMAIL_FROM=DeMentha <noreply@yourdomain.com>

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App Configuration
RESERVATION_FEE_CENTS=
