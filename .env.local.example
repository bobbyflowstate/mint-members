# Convex
NEXT_PUBLIC_CONVEX_URL=

# Auth (set these in Convex Dashboard or via `npx convex env set`)
# 
# JWT_PRIVATE_KEY - Required for signing auth tokens
#   Generate with: npx @convex-dev/auth
#   Or manually: openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -outform PEM | tr '\n' ' '
#   Then set in Convex: npx convex env set JWT_PRIVATE_KEY "your-key-here"
#
# SITE_URL - Your app's URL (for magic link redirects)
#   Development: http://localhost:3000
#   Production: https://yourdomain.com
#
# AUTH_RESEND_KEY - Resend API key for sending magic link emails
#   Get from: https://resend.com/api-keys
#
AUTH_EMAIL_FROM=DeMentha <noreply@yourdomain.com>

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App Configuration
RESERVATION_FEE_CENTS=

# Ops Portal Password
# Password required to access the /ops portal
OPS_PWD=
